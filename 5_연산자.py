"""
# 연산자 : 프로그램에서 값을 계싼하거나 변수에 대해 연산을 수랭하는 기호
# 산술연산자 : 사칙연산, 나머지 연산
i = 10
j = 4
print(i + j)
print(i - j)
print(i * j)
print(i / j)
print(i % j)
print(i // j)       # 몫
print(int(i / j))   # 이렇게 해도 몫
print(i ** j)

# 문자열 연산
text = "Python"
print(text + " Programming")  # 문자열 연결
print(text * 3)               # 문자열 반복
print("="*10 + "성적 정보" + "="*10)

#  대입 연산자
num = 10
print(num)   # 출력: 10

num += 2     # +=: 더하기 후 대입
print(num)   # 출력: 10 + 2 = 12

num -= 3     # -=: 빼기 후 대입
print(num)   # 출력: 12 - 3 = 9

num *= 2     # *=: 곱하기 후 대입
print(num)   # 출력: 9 * 2 = 18

num /= 3     # /=: 나누기 후 대입 (실수 나눗셈)
print(num)   # 출력: 18 / 3 = 6.0 (소수점이 붙은 float 형으로 변환)

num //= 4    # //=: 몫 구하기 후 대입 (소수점 이하 버림)
print(num)   # 출력: 6.0 // 4 = 1.0 (6.0을 4로 나눈 정수 몫)

num %= 2     # %=: 나머지 구하기 후 대입
print(num)   # 출력: 1.0 % 2 = 1.0 (1.0을 2로 나눈 나머지)


# 비교 연산자 : 결과가 참과 거짓으로 반환
a = 10
b = 20
print(a > b)
print(a < b)
print(a == b)
print(a != b)
print(a >= b)
print(a <= b)

# 논리연산자 : 참과 거짓을 반환
# and / or / not
x = 5
y = 10
print(x > 0 and x > y)
print(x > 0 or x > y)
print(not(x > 0 or x > y))

# 삼항연산자
age = 18
is_adult = "성인" if age > 19 else "미성년"
print(is_adult)
"""

# 윤년 계산하기
# - 연도가 4로 나누어 떨어진다.
# - 100으로 나누어 떨어지면 연도는 윤년이 아니다.
# 이유 설명. 0.2422일 * 4년 = 0.9688일을 1년으로 치기 때문에 0.0312일이 추가로 쌓이게 된다.
# 100년 동안 25번 윤년이 지나가면 0.0312일 * 25 = 0.78일이 더 쌓이는데, 이걸 막기 위해 100년마다 윤년을 빼준다고 한다.
# - 400으로 나누어 떨어지면 운년이다.
# 이유 설명. 이대 하루를 빼면 0.22일을 더 빼버린 격이 되기 때문에, 400년동안 4번의 윤년이 지나면 0.88일이 더 부족해 진다.
# 따라서 400년째가 되는 해에는 다시 윤년을 넣는다.

# 직접 풀어본거~~~
"""
year = int(input("년도를 작성하시오. "))
if (year % 4 == 0 and year % 100 != 0) or (year % 400 == 0):
    print(f"{year}년도는 윤년입니다.")
else:
    print(f"{year}년도는 윤년이 아닙니다.")
"""
# 아래는 문제 풀이 1,2,3단계
"""
# 1단계. 4로 나누어지는가
year = int(input("년도를 작성하시오."))
if year % 4 == 0: # 4로 나누어 떨어진다
    print(f("{year}년은 윤년 입니다."))
else:
    print(f"{year}년은 윤년이 아닙니다")
"""
"""
#  2단계. 4로 나누어지지만 100으로 나누어 지지 않는다
year = int(input("년도를 작성하시오."))
if year % 4 == 0 and year % 100 != 0: # 4로 나누어 떨어지지만, 100으로 나누어 떨어져선 안된다
    print(f("{year}년은 윤년 입니다."))
else:
    print(f"{year}년은 윤년이 아닙니다")
"""
"""
# 3단계. 4로 나누어지지만 100으로 나누어 지지 않는다, 그리고 400으로 나누어진다
year = int(input("년도를 작성하시오."))
if year % 4 == 0 and (year % 100 != 0 or year % 400 == 0): 
    # 4로 나누어 떨어지지만, 100으로 나누어 떨어져선 안된다, 그리고 400으로 나누어 떨어진다
    print(f("{year}년은 윤년 입니다."))
else:
    print(f"{year}년은 윤년이 아닙니다")
"""

# 100의 자리 정수를 입력받아 100의 자리, 10의 자리, 1의 자리로 나누어 합을 구하기
# 예 입력 : 987 >>> 출력 : 24

# it's me~~~
"""
number = int(input("3자릿수를 입력하시오. "))
print((number // 100) + (number % 100 // 10) + (number % 10))
"""
# 문제 풀이
"""
num = int(input("3자릿수를 정수 입력 "))
a = num // 100
b = num % 100 //10
c = num % 10
total = a + b + c
print(total)
"""

# 1. 대입연산자 실습
# # num = 50에서 시작해서 아래 연산을 순서대로 적용한 뒤, 각 단계마다 결과를 출력하세요.
# # 20을 더한다
# # 3배로 곱한다
# # 7로 나눈 나머지를 구한다
# # 2를 뺀다
# # 각 연산은 대입 연산자(+=, *=, %=, -=)를 사용해서 작성하세요.
"""
num = 50
print(num)
num += 20
print(num)
num *= 3
print(num)
num %= 7
print(num)
num -= 2
print(num)
"""

# 2. 논리연사자로 범위 판별 : 조건문 포함
# 사용자에게 점수(0~100)를 입력받아, 점수가 60점 이상 80점 미만이면 "보통"을 출력하고,
# 그 외에는 "보통 아님"을 출력하는 코드를 and, or, not 중 알맞은 논리연산자를 사용해서 작성하세요.

# it's me~~~
"""
num = int(input("0에서 100까지 임이의 점수를 입력해주세요. "))
if (num >= 60) and (num < 80):
    print("보통이여")
else:
    print("보통이 아녀!")
"""
# 문제 풀이
"""
score = int(input("점수입력 : "))
if 60 <= score <= 80:
    print("보통")
else:
    print("보통아님")
"""

# 3. 비교 연산자와 삼항 연산자
# 두 수 a, b를 사용자로부터 입력받아, 삼항 연산자를 이용해 둘 중 더 큰 값을 max_num이라는 변수에 저장하고 출력하는 코드를 작성하세요.
# (단, 두 수가 같을 경우 "두 수가 같습니다"를 출력)

# it's me~~~
"""
a, b = map(int, input("비교할 숫자를 두 개 입력하세요. ").split())
if a > b:
    print(f"max_nam : {a}")
elif a < b:
    print(f"max_nam : {b}")
else:
    print("우열을 가리 수 없습니다")
"""
# 문제 풀이
"""
a = int(input("첫 번째 수 입력 : "))
b = int(input("두 번째 수 입력 : "))
if a == b:
    print("두 수가 같습니다.")
else:
    max_num = a if a > b else b
    print(max_num)
"""

# 4. 아래와 같은 형태의 출력이 나오도록 문자열 반복(*)과 연결(+) 연산자를 사용해서 코드를 작성하세요.
# **********학생 명단**********
# 1. 홍길동
# 2. 김철수
# **********************************
"""
print("*" * 10 + "학생 명단" + "*" * 10)
print("1. 홍길동")
print("2. 김철수")
print("*" * 28)
"""

# 5. 짝수/홀수 판별과 나이 계산
# 사용자에게 태어난 연도(예: 2000)를 입력받아 다음을 계산하는 프로그램을 작성하세요.
# 현재 연도(2026)를 기준으로 만 나이를 계산 (2026 - 태어난 연도)
# 계산된 나이가 짝수인지 홀수인지 나머지 연산자(%)로 판별
# 삼항 연산자를 사용해서 "짝수" 또는 "홀수"를 변수에 저장한 뒤 다음과 같이 출력

# it's me~~~
"""
year = int(input("태어난 년도를 입력하세요. "))
age = 2026 - year
if age % 2 == 0:
    print("당신의 나이는 짝수 입니다")
else:
    print("당신의 나이는 홀수 입니다")
"""
# 현대 날짜를 가져와 문제 풀이
"""
from datetime import datetime
birth_year = int(input("태어난 년도를 입력하세요. "))
current_year = datetime.now().year
age = current_year - birth_year
rst = "짝수" if age % 2 == 0 else "홀수"
print(f"당신의 나이는 {age}세이며, {rst}입니다.")
"""
